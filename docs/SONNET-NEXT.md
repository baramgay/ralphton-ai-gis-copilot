# Sonnet 지시 — 2차 검증 (2026-07-28)

이 문서를 **별도 세션에서 Sonnet에게 그대로 붙여** 쓴다. `docs/SONNET-VERIFY.md`의
0절(절대 어기면 안 되는 것)과 3절(하지 말 것)은 그대로 유효하다. **코드는 고치지 않는다.**

앞 리포트(`docs/NL-QUERY-BUGS-2026-07-28.md`)는 품질이 좋았다. 같은 방식으로 계속한다.
재현·원인진단까지만, 판단이 필요한 건 "판단이 필요한 것"에 적는다.

---

## 앞 리포트 처리 결과 (참고)

**고침·배포·prod 검증 완료**: #1 군 축약 · #2 격자 · #4 비의료 시설 · #5 3지역 절단 ·
#6 "준" · #7 의료 교차 · #12 주말 · (+ 리포트에 없던 세대수 무응답)

**#12 원인은 추정과 달랐다**: LLM 의도분류가 아니라 `query-signals.ts`의 `"몰리"`가
**인구 증가 신호**로 등록돼 있었다. 나머지 원인진단은 전부 코드에서 확인됐다.

**#8은 진단이 틀렸다**: "1인가구 전용 지표 부재"가 아니라 **가로채기**였다.
`rankSingleHouseholdRisk`가 멀쩡히 등록돼 있는데 큐브 `"가구"` 트리거가 먼저 걸린 것이다.
이걸 믿고 차단 가드를 넣었다가 되돌렸다.

→ **교훈: "그 지표는 없다"고 쓰기 전에 `grep`으로 구현체를 찾아본다.** 못 찾았을 때만
"부재"라고 쓰고, 찾았으면 "가로채기"라고 쓴다. 둘은 고치는 방법이 정반대다.

---

## 1. 최우선 — 공공 도구의 시군구 단위 무시 (계통 결함)

이미 확인된 것(`.prod-sgg.mjs`):

```
✗ 총인구 많은 시군구      → 1위 창원시의창구 북면
✗ 고령비율 높은 시군구    → 1위 진주시 정촌면
✗ 세대수 많은 시군구      → 1위 의령군 낙서면
✗ 출생 많은 시군구        → 1위 창원시의창구 대산면
✗ 1인가구 많은 시군구     → 1위 창원시마산합포구 진전면
✗ 의료 취약한 시군구      → 1위 거창군 북상면
✓ 카드매출 높은 시군구    → 1위 진주시   (민간 큐브 경로)
✓ 생활인구 많은 시군구    → 1위 김해시   (민간 큐브 경로)
```

### 할 일
1. **공공 tool-registry의 모든 rank 도구**를 훑어라. `TOOL_CATALOG`에서 `id`가 `rank`로
   시작하는 것 전부. 위 6개 말고 몇 개가 더 있는지, 전부 같은 증상인지.
2. 각 도구마다 이 표현들을 다 시도하고 1위 이름이 시군구인지 읍면동인지 적어라:
   `"X 많은 시군구"` · `"시군구별 X"` · `"X 높은 시별"` · `"22개 시군구 X 순위"`
3. 원인 위치를 찾아라. 민간 경로는 되는데 공공 경로만 안 되므로, 두 경로가 `adminLevel`을
   어떻게 다르게 다루는지 비교하면 나온다. 시작점: `query-catalog.ts`의 `scopeFilters()`,
   `query-signals.ts`의 `detectAdminLevel` 계열, `tool-registry.ts`의 `scopedRegions()`.
4. **집계가 가능한지도 확인하라.** 단위만 바꾸면 되는지, 아니면 읍면동 값을 시군구로
   합산·가중평균하는 코드가 아예 없는지. 비율 지표(고령비율·1인가구 비율)는 단순 합이
   아니라 가중평균이어야 한다 — 그 코드가 있는지 확인해서 적어라.

이건 결과가 그럴듯해서 위험하다. "고령비율 높은 시군구"에 읍면동 순위를 주면 그대로
보고서에 들어간다.

---

## 2. 변주 코퍼스로 라우팅 실패율 측정

지금까지는 내가 생각해 낸 표현만 검증했다. 그래서 내가 생각 못 한 표현이 계속 나온다.
**기계적으로 변주를 만들어 실패율을 재라.**

### 방법
`catalog.ts`의 모든 지표에 대해, 지표 이름 하나당 아래 틀로 문장을 찍어낸다:

```
"{지표} 많은 동"          "{지표}가 많은 동"        "{지표}는 많은 동"
"{지표} 높은 곳"          "{지표} 낮은 곳"          "{지표} 적은 읍면동"
"{지표}로 봤을 때 상위"    "{지표} 기준 상위 5곳"     "어디가 {지표} 높아?"
"{지표} 상위 시군구"       "김해 {지표} 높은 동"      "{지표} 늘어나는 동"
```

각 문장에 대해 **안내에 그 지표 이름이 나오는지**만 본다(값 검증은 불필요).
나오지 않으면 실패. 실패한 것을 **지표별·틀별로 집계**해서 표로 내라.

산출물은 두 가지다:
- 어느 **지표**가 잘 안 잡히는가 (트리거 보강 대상)
- 어느 **틀**이 잘 안 잡히는가 (매칭 방식 문제)

두 번째가 핵심이다. 특정 틀이 지표 전반에서 실패하면 개별 트리거가 아니라 매칭 방식을
고쳐야 한다는 뜻이고, 그건 Opus가 판단한다.

**주의**: 지표가 40개가 넘으므로 전수는 시간이 오래 걸린다. 큐브 레이어당 대표 지표
2개씩으로 시작해서 실패율이 높은 레이어만 전수로 넓혀라. 몇 개를 돌렸는지 반드시 적어라.

---

## 3. 재현만 된 결함 3건의 원인 위치 추적

앞 리포트 #10·#11·#13이다. 재현은 확정됐고 코드 위치가 미특정이다.

| # | 질의 | 실제 |
|---|---|---|
| 10 | "상위 10% 소득 지역" / "소득 100만원 이상인 동" | 조건 무시, 기본 24행 |
| 11 | "카드매출이 늘지 않은 동" | 절대 수준 순위로 대체 |
| 13 | "노인 인구 비율 상승하는 곳" | "고령 비중 높은 동"과 동일 결과 |

각각에 대해:
- 어느 리졸버까지 갔다가 어디서 조건이 버려지는지 (`resolveTrendQuery`가 null을
  반환하는지, 아니면 조건 자체를 안 읽는지)
- #13은 특히 중요하다 — 추세 경로가 **있는데** 안 타는 것인지, 고령비율에 추세 지원이
  없는 것인지 갈라라. 전자면 라우팅 문제고 후자면 기능 부재다.

---

## 4. 회귀 (매번)

`docs/SONNET-VERIFY.md` 1절 그대로. 현재 기대값:

```
sweep 56/56 · values 46/46 · hard 실패 0 · round5 답11되물음1 · round6 14/14
round7 실패 0 · round8 실패 0 · verify-batch 12/12 · verify-b 19/19 · verify-c 16/16
tsc 0 · vitest 1,225 · eslint 0 errors(warning 7) · playwright 16 · JS 에러 0
```

**하나라도 어긋나면 그 자리에서 멈추고 보고한다.** 특히 `values`는 46이어야 한다 —
가드를 잘못 넣어 46 → 42로 떨어진 적이 있고, 그때 다른 검증은 전부 통과했다.

---

## 5. 부탁 하나

검증 스크립트(`.prod-*.mjs`) 27개가 전부 gitignore돼 있어 버전관리 밖에 있다.
**이번 세션의 결함을 전부 이 스크립트들이 잡았고 유닛테스트는 하나도 못 잡았다.**
새 스크립트를 만들면 파일명과 용도를 보고서에 한 줄씩 적어 둬라. 버전관리로 옮길지는
Opus가 판단한다(민간데이터 질의문이 들어 있어 공개 레포에 올리는 것이 맞는지 확인 필요).

---

## 보고 형식

`docs/SONNET-VERIFY.md` 4절 그대로. 파일로 남길 때는
`docs/NL-QUERY-BUGS-2026-07-28-2차.md`.

원인을 쓸 때 **"부재"인지 "가로채기"인지 반드시 갈라서** 적어라. 확신이 없으면
"확신도" 칸에 그렇게 쓰면 된다 — 앞 리포트의 그 칸이 실제로 도움이 됐다.
