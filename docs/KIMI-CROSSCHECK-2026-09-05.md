# Kimi 답변서 교차검증 — 2026-09-05

대상: `docs/KIMI-ANSWER-2026-09-04.md`
원칙: **답변서의 결론을 그대로 받지 않는다.** 주장마다 이쪽에서 다시 재고, 재현된 것만 고친다.

먼저 인용에 관한 것 하나. 답변서의 줄번호는 실제 파일과 맞지 않는다 — `aggregate.ts:549`를
가리키지만 그 파일은 88줄이고, `stats-view.ts:721`·`point-probe.ts:1107`도 마찬가지다(각각
247줄·275줄). 여러 파일을 이어 붙인 좌표로 보인다. 그래서 **인용은 버리고 주장만** 코드로
다시 확인했다. 결론부터: 인용은 어긋났지만 **주장은 대체로 맞았다.**

---

## 판정 요약

| 항목 | Kimi 판정 | 내 재현 | 조치 |
|---|---|---|---|
| 1. 상관 표본 22 | 거짓(18로 내려야) | **재현됨** | 고침 |
| 1-딸림. 축 간 연도 혼합 | 결함 | **재현됨** | 고침 |
| 2-2. z-표준화 창원 5/22 가중 | 결함 | **재현됨** | 고침 |
| 2-4(b). 한 지표 안 연도 혼합 | 결함 | **재현됨**(함양군 11개 읍면) | 고침 |
| 3-4. 「오차 미터 아래」 | 거짓 | **재현됨**(3km −16.8m) | 고침 |
| 4. MAD 판정이 뒤집힘 | 부분 거짓 | **재현됨** | 1번 수정으로 함께 해결 |
| 5. RAG top-5 100% | 과대포장 | **재현됨**(19/30·27/30, 누락 3건 동일) | 평가셋 승격 + 어휘 계열 보강(21/30·29/30) |
| 6-확정1. sum 결손 합산 | 결함 | **재현됨**(실데이터 4개 지표) | 고침 |
| 6-확정2. `changeRate ?? 0` | 결함 | **거짓** — 아래 참고 | 이웃 결함을 대신 고침 |
| 7. 상관 결과 「(0점)」 | 결함 | **재현됨** | 고침 |

---

## 1. 상관 표본 — 재현됨, 고침

실제 레이어 JSON으로 다시 쟀고 답변서의 세 값이 그대로 나왔다: 재정자립도×빈집
−0.859 → −0.754, 재정자립도×화재 −0.914 → −0.845, 빈집×화재 +0.798 → **+0.647**(0.151).

결정적인 논거는 계수 크기가 아니라 **자기모순**이었다. 화면은 창원을 한 줄로 접어 18줄을
보여 주면서 바로 밑에 「표본 22개」라고 적고 있었다. 같은 화면에서 표본이 18이기도 하고
22이기도 했던 것이다.

고친 방향은 답변서의 권고를 그대로 받았다 — 「창원을 1점으로」가 아니라 **「복제된 축을
값으로 알아본다」**. `src/lib/layers/independent-observations.ts`:

- 「…시 …구」인 이름만 후보로 삼는다. 읍면동·군은 **절대 접지 않는다**(진주시의 두 동이
  우연히 같은 값을 가질 수 있고, 그 둘은 서로 다른 관측이다).
- 같은 시의 자치구들이 **재는 값까지 모두 같을 때만** 한 관측으로 센다. 값이 다르면 구별
  자료가 실재한다는 뜻이라 그대로 둔다.
- 전국으로 넓혀도 수원·성남·고양에서 다시 깨지지 않는다.

검사는 실데이터로 걸었다(`tests/layers/independent-observations.test.ts`). 접기를 끄고 돌려
6건이 붉어지는 것까지 확인했다 — 초록불이 「위반 없음」인지 「검사가 못 봄」인지 가리기
위해서다.

## 1-딸림 · 2-4. 시점 혼합 — 재현됨, 고침

두 가지가 다르다.

- **축 사이**: 재정자립도는 2024-12가 최신, 화재 발생률은 2025-12가 최신이다. 그 상관은
  한 해 어긋난 두 값의 상관인데 화면이 아무 말도 하지 않았다.
- **한 지표 안**: 뺑소니율은 **함양군 11개 읍면만** 2024-12가 최신이고 나머지 294곳은
  2025-12다. 한 순위 안에 두 시점이 섞여 있었다.

이제 둘 다 화면에 적는다. 「기준 시점이 다릅니다 — …은 2024-12, …은 2025-12 값입니다」와
「…은 시군구마다 최신 시점이 다릅니다 — 11곳이 2024-12 값이고…」.

## 2-2. z-표준화 — 재현됨, 고침

`cross-analysis.ts`의 평균·표준편차가 22칸(창원 5구 포함)으로 계산돼 한 도시가 22.7%의
가중으로 평균을 끌고 있었고, 그 평균이 「경남 평균 이상」이라는 문구로 화면에 나왔다.
z를 만드는 기준만 독립 관측으로 내렸다 — 순위와 지도 채색은 22칸 그대로다(창원 각 구도
자기 z를 받아야 지도가 칠해진다).

## 3-4. 「몇 km에서 오차가 미터 아래」 — 거짓, 고침

수치가 그대로 재현됐다: 0.5km −2.8m · 1km −5.6m · **3km −16.8m** · 5km −27.9m. 곡률이
아니라 상수가 원인이다 — 근사는 110.574(적도 자오선 호)를 쓰는데 같은 카드의 시설 거리는
하버사인(R = 6371.0088km, 위도 1° = 111.195km)이었다. **한 원 안에서 두 자를 쓰고 있었다.**

두 상수를 같은 구에서 끌어오도록 고쳤다. 검사는 「같은 좌표에 놓은 시설과 경계까지의 두
거리가 1m 안에서 같은가」로 걸었고, 옛 상수로 되돌리면 3건이 붉어지는 것을 확인했다.

구 자체가 타원체와 다른 것(경도에서 약 0.2%)은 이 앱 전체가 이미 지고 있는 근사다. 여기서
중요한 것은 절대 정확도가 아니라 **같은 화면의 두 숫자가 같은 자로 재였는가**다.

## 4. MAD — 1번 수정으로 함께 해결

이상치도 같은 규칙으로 독립 관측을 센다. 답변서가 짚은 대로 방향이 **양쪽으로** 뒤집힌다 —
재정자립도는 중복이 창원 자신을 정상으로 만들었고(22점 이상치 없음), 병상은 반대로 MAD를
3.15 → 1.20으로 무너뜨려 22곳 중 8곳을 이상치로 인쇄했다.

## 5. RAG — 재현됨, 평가셋을 상시 검사로

외부 질의 30개를 이쪽 채점으로 다시 재서 **top-1 19/30 · top-5 27/30**, 5위 밖 3건도
동일(#8 병원 가기 힘든 읍면 · #14 한 반에 학생이 너무 많은 지역 · #18 사람이 계속 들어오는
동네). 답변서의 숫자가 그대로 나왔다.

먼저 고친 것은 검색이 아니라 **우리가 성능이라고 적은 문장**이었다. 15/15는 그 15개에
맞춰졌다는 뜻이지 성능이 아니다. 그래서 30개를 `tests/rag/independent-corpus.test.ts`로
옮겨 바닥선을 지키게 했다. 기대값은 손대지 않는다 — 우리 편한 대로 고치기 시작하면 이
셋도 첫 번째 셋과 같아진다.

그다음에 어휘 구멍 둘을 계열로 넓혀 **21/30 · 29/30**이 됐고 바닥선도 그만큼 올렸다
(아래 「못 봤다고 남긴 것」 참고).

## 6-확정1. sum 결손 — 재현됨, 고침

`aggregate.ts`의 sum 경로가 `?? 0`으로 결손을 0으로 더했다. 실데이터를 전수로 훑어 실제
해당하는 자리를 셌다: `kcb-migration.move_in` 시군구 1곳, `nh-demographics.card_sales` 2곳,
`nh-hourly.day_sales`·`night_sales` 3곳. 하나라도 비면 합계를 내지 않도록 바꿨다.

대가는 그 시군구가 「자료 없음」이 되는 것이다. 그편이 낫다 — 없는 것은 없다고 말할 수
있어도, 작게 적힌 값은 되돌릴 수 없다. 같은 저장소가 이미 1인가구에서 같은 판단을 해
두었다(`tool-registry.ts`의 `sumNullableSeries`). 여기만 예외였다.

## 6-확정2. `changeRate ?? 0` — 거짓

답변서는 `trend-view.ts`·`trend-cross.ts`·`copilot-app.tsx`가 「산출 불가」를 0%(보합
한가운데)로 정렬한다고 했다. **그 자리에 null은 도달하지 않는다** — `buildTrendRanking`이
`if (trend.changeRate === null) continue;`로 먼저 뺀다. 뒤의 `?? 0`은 죽은 방어 코드다.

다만 그 옆에 진짜 결함이 있었다. **뺀 개수를 말하지 않는다.** 실데이터에서
`nh-storetype.pub_share`는 305개 읍면동 중 **165곳**이 첫 달 0이라 절반 이상이 조용히 빠진
채로 순위가 인쇄되고 있었다(비율은 답변서가 든 예보다 훨씬 크다). 이제 뺀 개수를 세어
산식 각주에 적는다.

## 7. 상관 결과의 「(0점)」 — 재현됨, 고침

상관은 칠할 지도가 없어 `mapScore: 0`을 박았는데, 해석문이 그 0을 「1위 창원시(0점)」으로
인쇄했다. 없는 값을 0으로 메우는 6번과 같은 가족이다. 점수 타입을 `number | null`로 열고
상관·이상치에서는 null을 넘긴다. 그러면 해석문이 지표 값으로 대신 말한다 — 「1위 창원시
(30.6%)」. 점수 막대도 그리지 않는다(막대 길이가 곧 「이만큼」이라는 주장이라, 없는 점수로
그리면 거짓을 그린다).

---

## 답변서가 「못 봤다」고 남긴 것 — 그중 셋은 이제 봤다

**KOSIS itmId 원장 대조 — 25개 전건 통과.** 키가 있어 직접 물어봤다
(`scripts/verify-kosis-items.mjs`). 표마다 항목을 전부 받아, 우리가 집은 id가 무엇인지와
그 표의 다른 후보가 무엇인지를 같이 본다. 결과: 22개는 이름에 산식이 박혀 있고
(「주민만명당 화재발생건수 (A÷B×10000)」), 3개는 이름·표 이름으로 율이 확인된다
(주택소유가구비율 · 보급률 · 학급당 학생수). **분자·분모(T001·T002)를 집은 것은 하나도
없다.**

여기서 검사기 자체가 두 번 틀렸고 둘 다 고쳤다:

- 키를 「이름=값」으로 잘랐다. 이 키는 **"="로 끝난다** — 빈 키를 보내고 26개 지표가 전부
  「표 없음」으로 나왔다. 자료가 없는 것처럼 보였지만 없던 것은 키였다.
- 처음엔 표 이름만 맞으면 통과시켰다. 그래서 `fire_rate`의 itmId를 일부러 분자(T001,
  원본 건수)로 바꿔도 초록이었다 — 표 이름이 「주민만명당…」이라 율로 읽혔다. **지키려던
  바로 그 오집을 못 보는 검사**였다. 표에 산식 항목이 있으면 그 표의 율은 그 항목이라고
  못 박고, 다시 주입해 붉어지는 것을 확인했다(T001·T002 둘 다).

**인쇄 미리보기 — 사람 눈 없이 판정했다.** PDF 안의 한글은 글꼴 부분집합 코드라 읽을 수
없다. 그래서 읽는 대신 **비교했다**(`scripts/verify-a4-print.mjs`): 같은 보고서를 머리글
반복만 끈 채로 한 번 더 찍어 글자 그리기 명령 수를 센다. 정상본 1,199 · 대조본 1,192 —
둘째 쪽에 열 이름이 실제로 더 찍힌다. 곁들여 알게 된 것: 기본 설정(상위 20건)에서 보고서는
**2쪽**이다. 즉 머리글 반복은 장식이 아니라 늘 쓰이는 규칙이었다.

**RAG 어휘 취약 — 계열로 넓혀 21/30 · 29/30.** 「부족하다」와 「가기 힘들다」는 다른 어휘
계열인데 전자만 실려 있었다. 한 표현을 끼워 넣으면 다음 표현에서 또 밀리므로 거리·접근의
말맛을 계열로 넓혔다(이 도구의 산식에 이미 「최근접 거리」·「2km 무시설」이 들어 있다).
「한 반에」 계열도 같이 넓혔다. 30개 중 5위 밖은 하나 남았는데, 그것은 **우리 기대가 틀렸을
수 있는 건**이라 그대로 둔다.

## 아직 안 한 것

- **DeepSeek 잔액 0** — AI 파서는 규칙 기반 폴백만 도는 상태라 판정 대상에서 빠져 있다.
  충전하면 즉시 동작한다.
- **#18 「사람이 계속 들어오는 동네」** — 기대(kcb-migration)와 다른 답(skt-mobility
  유입인구)이 1위로 온다. 어느 쪽이 옳은지는 사람이 정할 일이라 기대를 고치지 않았다.
